# 计费、访问控制与 OAuth 配额

修改成本、Token/Key 白名单、额度窗口、采样对账或手动重置时读取。

- **原生 Images 计费**(`proxy_sse_parser.go`、`proxy_util.go:computeRequestCost`):原生 Images 的顶层 usage 保留普通 token 统计，并独立提取 text/image/cache 明细；`cached_tokens_details` 从含缓存的输入明细扣除后计费。已登记图片费率时以图片专项成本替代普通 token 成本，不重复累加 Responses tool cost。flare/sunburst 使用已登记官方费率，不套 Image 2 按尺寸估算。基础 `gpt-image-2.5` 官方独立费率尚未确认（2026-09-10 模型文档返回 404），不假定等同某变体，维持现有普通价格目录/自定义价格回退；该回退不代表已核实的图片专项费率。

- **OAuth 累计窗口边界校正**(`oauthcost/usage.go:reconcileWindow`):带有效用量的新采样确认同周期、未发生显著用量回退且上游重置时间仍在未来时，在本地周期滚动之前更新成本窗口 `ResetAt`。保留 `StartedAt`、`CountFromAt` 和已有累计，避免移动计数起点后漏掉已计日志。半窗口容差只用于识别周期，不用于永久锚住旧重置时间；旧采样或缺用量基线的快照不得借此改写边界。真正的周期滚动和用量回退继续按原规则清零。

- **OAuth 采样与持久化成本对账**(`storage/sql/oauth_quota_cost.go`、`CompareAndSwapOAuthUsage`):5h、周、自然月窗口共用同一机制；主动额度刷新及 Codex/Anthropic 被动采样在凭证 CAS 的渠道行锁内核对日志并提交，返回实际持久化累计。窗口把累计成本所覆盖的日志区间持久化为 `accounted_from`/`accounted_until`，对账只查询它与当前 `[max(StartedAt, CountFromAt), ResetAt)` 的**对称差**：移入的区间加、移出的区间减、减不成负。**不能按新边界全量重算**——日志保留期可能短于周/月额度周期，重算会把保留期外已累计的成本抹掉。区间为零值表示未知（新窗口、用量回退清零、快照 bootstrap），此时按当前边界重算一次并与既有累计**取较大值**：存活日志只是真实成本的下界。汇总按实际模型族、逐条微美元取整，与增量累计同源——取整语义必须复刻 `util.USDToMicroUSDSafe` 的 `math.Round`（双精度乘法后半数远离零）：Postgres 与 MySQL 的 `ROUND` 作用于双精度时是半数取偶，对正成本恒少算 0 或 1 微美元、方向单一不会抵消，且手动重置基线与对称差的 added 分支都不经过取较大值保护，少算会直接落进持久化累计；`microUSDSumExpr` 先把乘积转十进制再取整以纠正 tie 方向，SQLite 原生已是半数远离零。`gpt-reserve` 仅累计实际请求模型为 `gpt-reserve` 的日志，主 Codex 窗口排除该模型；响应模型可能是 Luna，不能用它判断额度归属。后续提交的日志继续在同一行锁下增量累计并维持该区间不变式。凭证 CAS 与日志增量记账写同一行，冲突是常态，重试有界并退避。Codex 主周额度换位复用原窗口身份迁移，不能按时长合并独立额度组。日志已按旧截止时间本地滚动、但新采样确认真实窗口时，暂定起点和截止一起校正；手动重置后保留成本时同时保留真实计数起点，迟到日志仍遵守重置截止点。普通 token/metadata 凭证 CAS 不执行成本对账；混合存储仅在权威 SQLite 对账后复制结果，副本不重复累计。

- **渠道倍率** `cost_multiplier`(≤0 归 1):× 标准成本 = `effective_cost`,写日志时快照到 `logs.cost_multiplier` 避免历史污染
- **Auth Token**:`cost_*_microusd`(微美元整数避浮点);`cost_limit` 是总限额,`cost_daily_*`/`cost_monthly_*` 按服务器本地自然日/自然月累计,任一限额启用时必须同时设正数 `max_concurrency`;仅 2xx 累加费用,失败只计次,允许「超额一个请求」;`CCLOAD_API_TOKENS` 启动预置
- **Auth Token 访问控制**(`model/auth_token.go`、`auth_service.go`):`allowed_models` 模型白名单(空=无限制);`allowed_channel_ids`+`channel_restriction_mode`(`allow` 白名单/`deny` 黑名单,空 mode 视为 allow,空列表始终无限制),`ChannelRestriction.Allows` 封装极性,选择链路走 `FilterAllowedChannels`;`max_concurrency` 令牌级并发上限(0=无限),`acquireTokenConcurrencySlot` 获取槽位
- **Key 模型白名单**(`model/config.go:APIKey.AllowsModel`+`proxy_forward.go`+`admin_types.go:normalizeAPIKeyAllowedModels`):`api_keys.allowed_models` 限定单个 Key 服务哪些模型,空/`*` 都是不限制。**和 Auth Token 的 `allowed_models` 同名但不同层**——一个管令牌能请求什么模型,一个管渠道内哪个 Key 能被选中,改代码前先认准层级。比对的是 `resolveChannelRoutingModel` 给出的**渠道逻辑模型**:模糊匹配之后、`redirect_model` 重定向之前,所以白名单填渠道模型名而不是上游模型名;保存时校验模型必须存在于该渠道 `models`(渠道声明通配模型除外),按渠道模型名归一大小写并去重,编码后上限 2000 字节。过滤发生在 Key 重试循环之前,某渠道所有 Key 都不服务该模型时返回 `ErrNoAPIKeyForModel` → **跳过该渠道**,不冷却、不记失败
- **渠道每日限额** `daily_cost_limit`(美元,0=无限);`CostCache` 内存缓存按天重置
- **OAuth 配额成本**(`internal/oauthcost/usage.go`+`app/oauth_quota_cost.go`+`sql/log.go:AddLogWithOAuthQuotaCost`):Codex/Anthropic/Antigravity/xAI 凭证 JSON 的 `quota_cost_usage.windows[]` 内持久化**每个上游额度窗口**的**标准成本**(不乘 `cost_multiplier`,与 `effective_cost` 无关),随日志写入在**同一事务**累加,失败即整条日志回滚。槽位身份是上游的 `limit_name|kind`(`oauthcost.Key`),**不是窗口时长**——同一时长可以对应多个互不相干的窗口(Antigravity 的 gemini/3p 周额度、Anthropic 的三个 7 天窗口),按时长归并必然错位。每个槽位带模型族 `Family`,`AddStandardCost` 按日志的实际上游模型(`ActualModel` 优先)逐槽判族累加:Antigravity 按模型名前缀分 `gemini`/`non_gemini`,Anthropic 的 `seven_day_sonnet`/`seven_day_fable` 各自成族,Codex 的 `codex-spark` 只吃 Spark 模型,主 `codex` 窗口使用 `FamilyCodex` 排除 Spark 和 `gpt-reserve`，reserve 窗口使用 `FamilyCodexReserve` 仅吃 `gpt-reserve`,其他未分族窗口才使用 `FamilyAll`。窗口边界只来自上游额度采样(月=28–31 天区间,按自然月+`ResetDay` 锚点推进)后由 `Reconcile` 对账,没采到就没有窗口、不累加;采样里消失的槽位丢弃,但**一个有效窗口都没采到时保留已累计数据**(缺信息≠窗口消失)。只有落在 `[max(StartedAt, CountFromAt), ResetAt)` 半开区间的日志计入,迟到的旧日志不会污染新周期。混合存储副本走 `AddLogReplica` 不累加,凭证由 `markChannelDirty`→`syncChannelReplica` 异步复制到主库
- **CodeBuddy 余额**：不参与 `oauthcost` 标准成本对账；个人渠道查询资源包，企业渠道查询 `get-enterprise-user-usage` 的 `limitNum-credit`，保留小数，`limitNum=-1` 为无限额度。安全快照持久化 `remain/used/total/unlimited`，列表复用管理账户进度条显示已用、总额和可用比例；无总额或无限额度时不画百分比。查询复用 OAuth 渠道的单个、批量和活跃刷新链路。
- **Codex 周窗口换位**(`oauthcost/usage.go:reconcileCodexWeeklyKey`):主额度可能从 `primary=5h,secondary=7d` 变成 `primary=7d` 或反向变化。对账前只迁移主 `codex` 的对应 7 天计数器,保留原累计和 `CountFromAt`,随后仍按采样回退/周期滚动规则判断是否清零;不把 5h、Spark 或其它额度组的成本移入周窗口。旧布局采样不得回写或通过缺失窗口清理删除较新的计数器
- **Codex 活跃额度身份决定通用窗口归属**(`app/codex_usage_headers.go:codexPassiveLimitScope`):通用 `X-Codex-Primary/Secondary-*` 与 SSE `rate_limits` 描述的是"本次请求实际计量的额度"，显式 `metered_limit_name` 优先，缺失时继承 `X-Codex-Active-Limit`；两者均缺失且实际发送模型为 `gpt-reserve` 时归属 reserve。其余归属由额度身份决定：为空、`premium`（主额度）或 `codex` 时才写入并替换主 `codex` 作用域；实测 `base_model_inference` 与 `gpt-reserve` 归属 `gpt-reserve`，通用窗口只更新 reserve，不能写入主额度；其他未知身份不写主作用域，也不按值另建分组。其他附加组只从明确命名的 `X-Codex-<分组>-*` / `additional_rate_limits` 块采集；HTTP 中活跃身份命中命名组时替换该组。不能用"同一响应里是否出现完全一致的附加组"证明别名——上游可省略附加块、改用内部代号或让字段略有差异，任一情况把 reserve 的低使用率写进主额度都会被 `oauthcost` 当成提前重置而清空累计。未知身份宁可丢弃被动样本，由下一次官方 `/wham/usage` 刷新补齐。主额度合法回退重置及周期滚动保持原有规则。
- **OAuth 配额采样完整性**:Anthropic 被动响应和 xAI 周/月端点部分失败走 `ReconcilePartial`,保留未返回窗口;完整快照也不得删除采样时间比它更新的窗口。退休截止取有效窗口 `SampledAt` 与请求完成时间的最早值,避免 Codex reset credits/Anthropic profile 请求延迟掩盖旧额度采样。Codex 展示合并按单窗口采样时间判新旧,只匹配官方已有的同 key 窗口;同正时长的同周期抖动保留官方边界,旧周期已结束且被动样本落在向前推进的新周期内时,同步采用新比例及重置时间以关联新累计成本。套餐或上游策略引起时长变化时,较新被动样本须落在自身 `[reset_at-limit_window_seconds, reset_at)` 内,同时更新时长、比例和重置时间,允许新截止早于旧截止;若官方同额度组的另一槽位已有该新时长,则保留原布局等待完整快照确认,避免把槽位换位变成重复额度。不新增被动独有窗口,不合并不同额度组;旧样本、未知边界、已结束或尚未开始的跨时长周期不能覆盖官方值
- **渠道列表额度同步**:列表加载成功后更新已有 ready 额度缓存,让被动比例与累计金额持续更新;仅接受最新发起的列表请求,且不得覆盖请求期间完成的手动/自动额度刷新或正在进行的重置。每次普通列表加载都重新采样当日活跃渠道(切换筛选、翻页、手动刷新列表均再次触发),`activeChannelUsageAutoRefreshPendingIDs` 只跳过同一渠道尚未返回的在途请求,不得记录"本会话已刷新"之类的跨次去重——额度窗口会滚动,缓存快照不能代替重新采样。单个 OAuth 额度刷新、Codex 额度重置和手动批量额度刷新完成后,列表重载传入 `refreshUsage: false`,避免级联触发整页自动额度刷新;普通列表加载仍保留自动刷新及失败后重试。同步只替换额度数据,保留操作状态,不清理或重算历史成本
- **检测额度与成本**:定时检测、手动检测及管理对话复用普通请求的 Codex/Anthropic 被动额度采样规则；检测中的 Codex 响应体采样同步保存，保证额度窗口先于检测日志落库，首次检测成本也能计入。检测日志按实际上游模型归属额度窗口，标准成本仍由日志事务统一累加。
- **Codex 手动配额重置**(`admin_codex_quota_reset.go`,`POST /admin/channels/:id/codex-quota-reset`):先查上游 reset credit,无可用额度返回 409;credit 一经消费不可退,之后的本地清理(成本窗口重置、渠道全部冷却清除、额度刷新)全部 best-effort,失败只进响应 `warnings` 而不回滚。同渠道并发重置由 `codexQuotaResetInFlight` 直接 409 拒绝。重算标准成本必须先将每条日志取整为微美元再求和,与增量累计保持一致
- **自定义模型价格**(`util/custom_model_pricing.go`,设置键 `model_custom_pricing`):按模型 ID **整份替换**系统价格,不是增量合并。可表达的字段就是 8 个价目:基础四价(`input_price`/`output_price`/`cache_read_price`/`cache_write_price`)与高上下文四价(`input_price_high`/`output_price_high`/`cache_read_price_high`/`cache_write_price_high`,阈值由 `getTierThresholdForModel` 按模型名前缀内置)。固定按次费率(`fixed_cost_per_request`)只由系统目录维护(图像模型),自定义契约不提供该字段,`validateCustomModelPricing` 对非零值直接报错——否则程序化调用能塞进一个前端无法编辑、却覆盖系统图像计费的值。缓存价与缓存创建价都没有启用开关,也不接受布尔字段:填了(含显式 0)就按填的值算,留空则按基础价 × 系列倍率回退;1 小时缓存创建价恒为「生效输入价 × 2」,不单独配置。**只有真正的多档分层表不可覆盖**:三档及以上(或末档带真实上限,如 `qwen-flash` 的 1M)的表无法用两档表达,而 Qwen 全系(`qwen3-max`/`qwen3-vl-*`/`qwen3-coder-*` 等)基础价为 0、价格只存在于分层表里,一旦允许覆盖就会丢掉中间档导致计费归零或降档。`validateCustomPricingTarget` 在 `ParseCustomModelPricing` 与 `InstallCustomModelPricing` 两个入口都拒绝覆盖这类模型,前端在预填默认价时同样跳过并提示。**两档且末档无上限的表会在快照构建时折叠**成基础价 + 高上下文价(`collapseTwoTierHighContextPricing`):内置目录(`buildModelPricingSnapshot`)与远端 overlay(`overlayRemotePricing`)两条路径都必须折叠,否则同一模型的可覆盖性会在目录同步前后翻转。**折叠只换表达方式,不改计费语义**:GPT-5.6/GPT-6 系(以及 gpt-5.4/5.5/grok-4.5)长上下文仍是**分段计费**——超过阈值后整个请求改按高价算(上游 `tier.type=context`),不是前段按基础价、超出部分按高价累加;两条路径在阈值边界、缓存读、缓存创建上逐点等价。折叠的硬前提是档位边界等于 `getTierThresholdForModel` 推导出的阈值(否则阈值静默漂移),因此 GPT-5.6/GPT-6 系在 `getTierThresholdForModel` 里必须有 272K 分支——加新前缀模型时两处要同时改。折叠保留 `CacheReadCountsTowardTier`,OpenAI 系正是靠它把缓存读计入阈值判定。分段行为本身由 `TestCalculateCost_OpenAIContextTieredPricing` 断言,与表达方式无关。**图像费率不参与自定义**:图像字段的解析、校验、序列化与前端编辑全部移除,`CalculateImageGenerationToolCost`/`CalculateImageGenerationToolFallbackCost` 只查系统目录(`imageGenerationToolPricingByModel`/`imageGenerationFallbackCostByModel`),避免自定义覆盖把图像成本静默归零。前端 `projectCustomPricingDefaults` 在预填系统价格时按契约字段白名单投影,系统内部字段(`token_pricing_tiers`/`has_cache_read_price`/`cache_read_counts_toward_tier`/图像字段)不会混进提交体。改这里时别只看 `ModelPricing` 结构体——`TokenPricingTiers`、`CacheReadCountsTowardTier`、`HasCacheReadPrice`、图像字段仍是系统内置机制,只是不再对外暴露为可配置字段
- **自定义价格分档语义**：覆盖价目时，从当前系统目录（包括别名与前缀匹配）保留 `CacheReadCountsTowardTier`，目录刷新重建快照时重新取值。`input_price_high` 显式为 0 仍启用高档，由内部 `HasInputPriceHigh` 保留存在状态；未填写且值为 0 才不启用。两档系统表折叠时同样保留该状态，保证免费高档仍按原表计费。
- **定价细节**(service_tier 倍率、GPT-5.4/Qwen-Plus 分层降档、Gemini 长上下文翻倍、缓存读折扣/写乘数):读 `cost_calculator.go`
