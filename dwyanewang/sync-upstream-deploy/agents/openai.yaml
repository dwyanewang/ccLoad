interface:
  display_name: "同步三端上游并部署"
  short_description: "同步官方上游、本地与 origin master，并原子重建部署 ccLoad"
  default_prompt: "使用 $sync-upstream-deploy 同步官方上游、本地 master 与 origin/master，并原子重建 rw-main 后部署 Docker。"
